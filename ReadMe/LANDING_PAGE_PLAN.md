# 🚀 DEX Marketing — خطة تطوير Landing Page

## الوضع الحالي

الصفحة الحالية عبارة عن **Coming Soon Page** تحتوي على:
- خلفية 3D (رائد فضاء GLB model + نجوم + particles)
- عنوان رئيسي: "Fasten your seatbelts, we're in for a bumpy ride"
- عداد تنازلي (Countdown)
- زر "Get me Notified!"
- قائمة همبرجر جانبية
- تأثيرات سينمائية (vignette, scan-lines, film grain)

---

## نظام المحتوى الديناميكي (CMS) 📡

> **المحتوى كله بيتغير ديناميكياً من لوحة التحكم (Admin Panel) — مش hardcoded**

### قاعدة البيانات (Supabase)

| الجدول | الوظيفة | الأعمدة الأساسية |
|--------|---------|------------------|
| `site_settings` | إعدادات الموقع (ألوان، تواصل، سوشيال) | `key`, `value (JSONB)`, `type` |
| `pages` | صفحات بمحتوى ثنائي اللغة | `slug`, `title_en/ar`, `content_en/ar (JSONB)`, `is_published` |
| `portfolio` | أعمال/مشاريع | `title_en/ar`, `description_en/ar`, `images (JSONB)`, `category`, `is_featured` |
| `team_members` | أعضاء الفريق | `name_en/ar`, `position_en/ar`, `bio_en/ar`, `photo_url`, `display_order` |

### الصفحات المسجلة في الـ pages table

| slug | نوع المحتوى (JSONB shape) |
|------|--------------------------|
| `home` | `{ hero_title, hero_subtitle, cta_text }` |
| `about` | `{ mission, vision, story }` |
| `services` | `{ items: [{ id, title, description, image }] }` |
| `portfolio` | `{ items: [{ id, title, description, media }] }` |
| `contact` | `{ email, phone, address, whatsapp }` |
| `terms` | `{ title, content, last_updated }` |
| `privacy` | `{ title, content, last_updated }` |

### لوحة التحكم (Admin Panel) — **مبنية بالكامل ✅**

| Component | الملف | الوظيفة |
|-----------|-------|---------|
| `PagesManager` | `src/components/admin/pages-manager.tsx` | محرر صفحات كامل — ثنائي اللغة — مع رفع ميديا |
| `ThemeEditor` | `src/components/admin/theme-editor.tsx` | تغيير ألوان الثيم مع معاينة حية |
| `ContactSettings` | `src/components/admin/contact-settings.tsx` | تعديل بيانات التواصل والسوشيال |
| `MediaUpload` | `src/components/admin/media-upload.tsx` | رفع صور/فيديو عبر Cloudinary |

### Hooks (Client-Side — TanStack Query) — **مبنية بالكامل ✅**

```typescript
// src/hooks/use-cms.ts — 396 سطر
useSiteSettings()          // قراءة إعدادات الموقع
useUpdateSiteSetting()     // تحديث إعداد

usePages()                 // كل الصفحات
usePage(slug)              // صفحة بالـ slug
useCreatePage()            // إنشاء صفحة
useUpdatePage()            // تحديث صفحة
useDeletePage()            // حذف صفحة

usePortfolio()             // كل الأعمال
useCreatePortfolioItem()   // إضافة عمل
useDeletePortfolioItem()   // حذف عمل

useTeamMembers()           // أعضاء الفريق
useCreateTeamMember()      // إضافة عضو
useUpdateTeamMember()      // تعديل عضو
useDeleteTeamMember()      // حذف عضو
```

### Data Flow

```
Supabase DB ──→ Server Actions (RSC) ──→ SiteSettingsProvider ──→ Navbar/Footer
                                    └──→ Page Components (SSR)

Supabase DB ──→ TanStack Query Hooks ──→ Admin Panel (CRUD)
            ←── Mutations ←──────────┘

Supabase DB ──→ TanStack Query Hooks ──→ Landing Sections (CSR)
                 (use-cms.ts)              ServicesSection
                                           PortfolioSection
                                           etc.
```

### ⚠️ مشكلة معروفة

`getSiteSettings()` حالياً **بترجّع defaults ثابتة** مش من الـ DB — بسبب مشكلة RLS recursion في Supabase:
```typescript
// src/lib/actions/get-site-settings.ts
// TODO: Fix RLS policy infinite recursion in Supabase dashboard
return defaultSettings // ← مش بيقرأ من الـ DB
```

### تأثير الـ CMS على كل قسم في الـ Landing Page

| القسم | مصدر البيانات | الحقول الديناميكية |
|-------|--------------|-------------------|
| **Hero** | `pages` (slug: `home`) | `hero_title`, `hero_subtitle`, `cta_text` (EN/AR) |
| **About** | `pages` (slug: `about`) | `mission`, `vision`, `story` (EN/AR) |
| **Services** | `pages` (slug: `services`) | `items[]` — كل خدمة: `title`, `description`, `image` (EN/AR) |
| **Portfolio** | `portfolio` table + `pages` | أعمال: `title`, `description`, `images`, `category`, `is_featured` (EN/AR) |
| **Stats** | `site_settings` أو `pages` | أرقام ديناميكية — عدد العملاء/المشاريع/إلخ |
| **Testimonials** | يحتاج جدول جديد أو `pages` (slug: `testimonials`) | `items[]` — `name`, `position`, `company`, `text`, `avatar` |
| **Contact** | `site_settings` + `pages` (slug: `contact`) | `phone`, `email`, `address`, `whatsapp`, `social_*` |
| **Navbar/Footer** | `site_settings` | `site_name`, `site_logo`, `contact_*`, `social_*` |

---

## الهوية البصرية

| العنصر | القيمة |
|--------|--------|
| **اسم البراند** | DEX |
| **الأيقونة** | رائد فضاء (Astronaut) |
| **اللون الأساسي (غامق)** | `#022026` — Deep Space Teal |
| **اللون الثانوي (أصفر)** | `#F2CB05` — Gold / Amber |
| **لون النص** | `#FFFFFF` — أبيض |
| **الخطوط** | Tajawal (عربي) / Playfair Display (عناوين) / Space Mono (أرقام) |

---

## هيكل الصفحة الكامل (Full Landing Page)

الصفحة هتتكون من **8 أقسام** رئيسية:

---

### 1. 🎬 Hero Section (القسم الرئيسي) — **موجود حالياً ✅ — يحتاج تعديلات جوهرية**
> الشاشة الأولى اللي بتظهر — Full Screen

**المكونات:**
- خلفية 3D: رائد فضاء يطفو في الفضاء مع نجوم و particles
- العنوان الرئيسي: "Fasten your seatbelts, we're in for a bumpy ride"
- "Launching soon..." بالأصفر
- عداد تنازلي (أيام / ساعات / دقائق / ثواني)
- زر "Get me Notified!" مع سهم
- لوجو DEX أعلى يسار
- همبرجر منيو على الشمال
- تأثيرات (vignette, scan-lines, film grain)
- "Saturn V.2 // System Initialization" — نص جانبي ديكور

---

#### 🧑‍🚀 تعديلات رائد الفضاء (Astronaut Rework) — **أولوية عالية 🔴**

> **المشكلة الحالية:** رائد الفضاء مزقوق على الشمال وتحت أوي، شكله مش بارز ومش ماشي مع التصميم المرجعي.

**الوضع الحالي (المشاكل):**
| العنصر | القيمة الحالية | المشكلة |
|--------|---------------|---------|
| المكان X | `-2.2` (أقصى يسار) | مش في مكانه — لازم يمين/وسط |
| المكان Y | `-4.8` (تحت أوي) | بيقطع أغلب الجسم، مش باين كويس |
| Scale | `2.5` | محتاج يكبر شوية |
| دوران Y | `0.4` rad | محتاج يبقى مواجه أكتر |
| الكاميرا | `[0, 0, 3.5]` FOV 45 | محتاج تعديل بسيط |

**التعديلات المطلوبة على `Astronaut.tsx`:**

##### أ) إعادة تمركز رائد الفضاء (Position & Scale)
```
المطلوب:
- Position: [1.0, -3.5, -0.5]   ← يمين الوسط، أعلى، أقرب شوية للخلف
- Scale: 3.0                      ← أكبر عشان يملى الشاشة زي الصورة
- Rotation Y: 0.15                ← مواجه أكتر للمشاهد (مش ملتفت كتير)
```

##### ب) تحسين الإضاءة (Lighting Upgrade)
```
المطلوب:
- Key Light:    intensity 200 → أقوى، لون #f0f4ff (أبيض مزرق بارد)
- Fill Light:   intensity 150 → أقوى، لون #c026d3 (بنفسجي/ماجنتا أعمق)
                 ← ده اللي بيعمل الانعكاس البنفسجي على الخوذة
- Rim Light:    intensity 80  → أقوى، لون #F2CB05 (ذهبي) — يبقى أوضح
- Visor Glow:   إضافة PointLight جديد قدام الخوذة مباشرة
                 لون #e879f9 (بنفسجي فاتح)، intensity 30
                 ← عشان الخوذة تلمع من جوا زي الصورة
```

##### ج) إضافة هالة خلف الرأس (Head Glow/Halo) — **🆕**
```
المطلوب:
- دائرة مضيئة خلف رأس رائد الفضاء
- Sprite أو Plane مع texture دائري gradient
- لون: أبيض مائل للأزرق مع fade out
- حجم: 2x حجم الرأس تقريباً
- ده بيعمل تأثير backlighting دراماتيكي زي الصورة
```

##### د) تحسين مادة الخوذة (Visor Material)
```
المطلوب:
- envMapIntensity: 5.0    ← (كان 3.0) — انعكاسات أقوى
- إضافة emissive: #8b00ff  ← توهج بنفسجي من الخوذة نفسها
- emissiveIntensity: 0.3   ← خفيف بس ملحوظ
- clearcoat: 1.0 ✅ (مظبوط)
```

##### هـ) تحسين حركة الطفو (Float Animation)
```
المطلوب:
- الحركة الحالية: sin(t*0.4) * 0.1 ← سريعة ومش واضحة
- المطلوب: sin(t*0.25) * 0.15 + cos(t*0.15) * 0.05
  ← أبطأ وأكبر مع حركة ثانوية عشان يبان طبيعي أكتر
- إضافة rotation طفيفة: rotation.z = sin(t*0.2) * 0.02
  ← ميلان خفيف يمين وشمال عشان يحس بانعدام الجاذبية
```

**التعديلات المطلوبة على `SceneCanvas.tsx`:**

##### و) تعديل الكاميرا
```
المطلوب:
- Camera position: [0, 0.3, 4.0]    ← أبعد شوية وأعلى قليلاً
- FOV: 40                            ← أضيق = الرائد يبان أكبر وأقرب
- Bloom intensity: 0.7               ← (كان 0.5) أقوى عشان التوهجات تبان
- Bloom threshold: 0.15              ← (كان 0.2) يلقط توهجات أكتر
```

**التعديلات المطلوبة على `hero-overlay.tsx`:**

##### ز) تعديل layout النص
```
المطلوب:
- تغيير items-end → items-start في الـ container
  ← عشان النص يروح الشمال ورائد الفضاء يبقى على اليمين
- النص حالياً بيروح يمين ← لازم يروح شمال
- max-w-lg بدل max-w-2xl ← عشان ياخد مساحة أقل ويسيب مكان للرائد
```

---

**الملفات المتأثرة:**
| الملف | التغيير |
|-------|---------|
| `src/components/scene/Astronaut.tsx` | Position, Scale, Rotation, Lighting, Visor, Halo, Float |
| `src/components/scene/SceneCanvas.tsx` | Camera, Post-processing |
| `src/components/landing/hero-overlay.tsx` | Layout alignment, text width |

---

**مصدر البيانات (CMS) 📡:**
- جدول `pages` (slug: `home`) → `content_en.hero_title`, `content_en.hero_subtitle`, `content_en.cta_text`
- نفس الحقول بالعربي في `content_ar`
- بيتعدل من الـ Admin Panel → PagesManager

**التعديلات الأخرى على الـ Hero:**
- [ ] ربط النصوص بـ `usePage('home')` hook بدل hardcoded text
- [ ] اللوجو يتجاب من `site_settings` (key: `site_logo`)
- [ ] الهمبرجر منيو يفتح sidebar navigation
- [ ] إضافة smooth scroll للأقسام التالية
- [ ] تحسين الـ responsive للموبايل
- [ ] إضافة loading state أنيق للـ 3D model
- [ ] على الموبايل: الرائد يبقى خلف النص بـ opacity أقل أو أصغر

---

### 2. 🏢 About Section (من نحن) — **جزئي — صفحة مستقلة موجودة ⚠️**
> تعريف سريع بشركة DEX

> **ملاحظة:** صفحة `/about` موجودة فعلاً في `src/app/[locale]/about/page.tsx` بمحتوى كامل (inline SSR) — بس محتاج component منفصل للـ Landing Page الرئيسية كقسم ضمن الـ scroll.

**المكونات:**
- عنوان: "من نحن" / "About DEX"
- فقرة وصفية قصيرة عن الشركة ورؤيتها
- أيقونات/أرقام إنجازات (عدد العملاء، المشاريع، سنوات الخبرة)
- صورة جانبية أو animation بسيط
- تأثير parallax خفيف

**مصدر البيانات (CMS) 📡:**
- جدول `pages` (slug: `about`) → `{ mission, vision, story }` (EN/AR)
- الـ Hook: `usePage('about')` من `use-cms.ts`
- بيتعدل من Admin Panel → PagesManager

**التصميم:**
- خلفية: `#022026` gradient خفيف
- النصوص: أبيض مع لمسات `#F2CB05`
- Animation: fade-in on scroll (Framer Motion)

---

### 3. 🛠️ Services Section (خدماتنا) — **مطلوب إنشاؤه 🆕 — مرجع في صفحتين**
> عرض خدمات التسويق

> **ملاحظة:** مستوردة في `(website)/page.tsx` + `services/page.tsx` — بتكسر الـ Build ❌

**المكونات:**
- عنوان: "خدماتنا" / "Our Services"  
- Grid من الكروت (3-4 خدمات في الصف)
- كل كارت يحتوي على:
  - أيقونة/رسمة (Lucide icons أو custom SVG)
  - اسم الخدمة
  - وصف مختصر
  - زر "اعرف أكتر"
- تأثير hover: glow بالأصفر + scale خفيف

**مصدر البيانات (CMS) 📡:**
- جدول `pages` (slug: `services`) → `{ items: [{ id, title, description, image }] }` (EN/AR)
- الـ Hook: `usePage('services')` من `use-cms.ts`
- الأدمن بيضيف/يعدل/يحذف خدمات من PagesManager (items-array editor)
- الصور بترتفع عبر Cloudinary (MediaUpload component)

**الخدمات (defaults — بتتغير من الأدمن):**
1. **التسويق الرقمي** — إدارة حملات السوشيال ميديا والإعلانات
2. **تصميم الهوية البصرية** — لوجوهات وبراندينج كامل
3. **تطوير المواقع والتطبيقات** — مواقع وتطبيقات حديثة
4. **إنتاج المحتوى** — فيديوهات، تصوير، موشن جرافيك
5. **تحسين محركات البحث (SEO)** — ظهور أعلى في Google
6. **إدارة العلاقات العامة** — PR وتغطية إعلامية

**التصميم:**
- خلفية: أغمق شوية من الـ hero `#011a1f`
- الكروت: `#022026` مع border خفيف `#F2CB05/20`
- Hover: `border-[#F2CB05]` + `shadow-[#F2CB05/30]`

---

### 4. 💼 Portfolio Section (أعمالنا) — **مطلوب إنشاؤه 🆕 — مرجع في صفحتين**
> عرض نماذج من الأعمال السابقة

> **ملاحظة:** مستوردة في `(website)/page.tsx` + `portfolio/page.tsx` — بتكسر الـ Build ❌

**المكونات:**
- عنوان: "أعمالنا" / "Our Work"
- فلتر حسب النوع (All, Branding, Web, Social Media, Video)
- Grid/Masonry layout للمشاريع
- كل مشروع: صورة + اسم + فئة
- Modal/Lightbox عند الضغط على المشروع
- تأثير hover: overlay مع معلومات المشروع

**مصدر البيانات (CMS) 📡:**
- **جدول `portfolio` مستقل** — مش JSONB — جدول كامل بأعمدة:
  - `title_en/ar`, `description_en/ar`, `images (JSONB)`, `category`, `is_featured`
- الـ Hook: `usePortfolio()` من `use-cms.ts`
- الأدمن: `useCreatePortfolioItem()` / `useDeletePortfolioItem()`
- الصور بترتفع عبر Cloudinary
- RLS: أي حد يقرا — admin بس يكتب

**التصميم:**
- خلفية: `#022026`
- الصور: مع overlay gradient
- الفلتر: tabs بالأصفر `#F2CB05`

---

### 5. 📊 Stats/Counter Section (أرقام وإنجازات) — **مطلوب إنشاؤه 🆕**
> أرقام مؤثرة عن الشركة

**المكونات:**
- خلفية مميزة (parallax أو gradient مختلف)
- 4 أرقام رئيسية:
  - عدد العملاء: `150+`
  - المشاريع المكتملة: `500+`
  - سنوات الخبرة: `5+`
  - أعضاء الفريق: `30+`
- أنيميشن عداد (count-up animation on scroll)

**مصدر البيانات (CMS) 📡:**
- ممكن يبقى في `site_settings` (keys: `stat_clients`, `stat_projects`, `stat_years`, `stat_team`)
- أو ضمن `pages` (slug: `home`) → `content.stats[]`
- الأدمن يقدر يغير الأرقام من لوحة التحكم

**التصميم:**
- خلفية: gradient من `#022026` لـ `#033640`
- الأرقام: `#F2CB05` بخط كبير bold
- النصوص: أبيض

---

### 6. 💬 Testimonials Section (آراء العملاء) — **مطلوب إنشاؤه 🆕 — مرجع في صفحة**
> شهادات ورأي العملاء

> **ملاحظة:** مستوردة في `(website)/page.tsx` — بتكسر الـ Build ❌

**المكونات:**
- عنوان: "ماذا يقول عملاؤنا" / "What Our Clients Say"
- Carousel/Slider من الشهادات
- كل شهادة: صورة العميل + الاسم + المنصب + الشركة + النص
- تأثيرات الانتقال السلس
- Auto-play مع pause on hover

**مصدر البيانات (CMS) 📡:**
- ممكن ضمن `pages` (slug: `testimonials`) → `{ items: [{ name, position, company, text, avatar }] }` (EN/AR)
- أو يحتاج جدول `testimonials` جديد في Supabase لو عايزين CRUD كامل
- الأدمن يضيف/يعدل الشهادات من PagesManager

**التصميم:**
- خلفية: `#011a1f`
- Card: شفاف مع border
- Quote mark: `#F2CB05`

---

### 7. 📩 Contact Section (تواصل معنا) — **جزئي — صفحة مستقلة موجودة ⚠️**
> فورم التواصل + معلومات الاتصال

> **ملاحظة:** صفحة `/contact` موجودة فعلاً في `src/app/[locale]/contact/page.tsx` بفورم كامل (inline SSR) — بس محتاج component منفصل للـ Landing Page.

**المكونات:**
- عنوان: "تواصل معنا" / "Get in Touch"
- صف مقسوم:
  - **يسار:** فورم (الاسم، الإيميل، رقم الموبايل، الرسالة)
  - **يمين:** معلومات الاتصال + خريطة
- معلومات الاتصال:
  - 📍 العنوان
  - 📞 رقم الهاتف
  - ✉️ الإيميل
  - 🕐 ساعات العمل
- روابط السوشيال ميديا
- زر إرسال مع animation

**مصدر البيانات (CMS) 📡:**
- `site_settings` → `contact_phone`, `contact_email`, `contact_address_en/ar`, `social_*`
- جدول `pages` (slug: `contact`) → `{ email, phone, address, whatsapp }` (EN/AR)
- الـ Hooks: `useSiteSettings()` + `usePage('contact')` من `use-cms.ts`
- الأدمن يعدل من ContactSettings + PagesManager

**التصميم:**
- خلفية: `#022026`
- الفورم: inputs مع border خفيف وfocus بالأصفر
- زر الإرسال: `bg-[#F2CB05]` مع `text-[#022026]`

---

### 8. 🦶 Footer Section (التذييل) — **مطلوب إنشاؤه 🆕 — مستورد في 7 صفحات ❌**
> تذييل الصفحة

> **ملاحظة:** مستورد في 7 صفحات بس الملف مش موجود = Build مكسور

**المكونات:**
- لوجو DEX
- روابط سريعة (من نحن، خدماتنا، أعمالنا، تواصل معنا)
- روابط السوشيال ميديا (Facebook, Instagram, LinkedIn, Twitter/X, TikTok)
- Copyright text
- "صنع بـ ❤️ بواسطة DEX"

**مصدر البيانات (CMS) 📡:**
- `site_settings` → `site_name`, `site_logo`, `social_facebook`, `social_instagram`, `social_twitter`, `social_linkedin`
- `site_settings` → `contact_phone`, `contact_email`
- الـ Hook: `useSiteSettings()` من `use-cms.ts`
- أو `useSiteSettingsContext()` من SiteSettingsProvider (لو SSR)
- الأدمن يعدل من ContactSettings + ThemeEditor

**التصميم:**
- خلفية: `#011118` (أغمق من كل شي)
- النصوص: `white/60`
- الروابط: hover بالأصفر

---

### 9. 📍 Navbar (شريط التنقل) — **مطلوب إنشاؤه 🆕 — مستورد في 7 صفحات ❌**

> **ملاحظة:** مستورد في 7 صفحات بس الملف مش موجود = Build مكسور

**المكونات:**
- لوجو DEX (يسار)
- روابط التنقل (الرئيسية، من نحن، خدماتنا، أعمالنا، تواصل معنا)
- زر تغيير اللغة (AR/EN)
- زر CTA ("تواصل معنا" / "Contact Us")
- Floating/Sticky عند الـ scroll
- شفاف في البداية → يبقى solid عند الـ scroll

**مصدر البيانات (CMS) 📡:**
- `site_settings` → `site_name`, `site_logo`
- الـ Hook: `useSiteSettingsContext()` من SiteSettingsProvider

**التصميم:**
- خلفية: transparent → `#022026/90` backdrop-blur
- النصوص: أبيض
- CTA button: `bg-[#F2CB05]` مع `text-[#022026]`

---

### 10. 📢 CTA Section (Call to Action) — **مطلوب إنشاؤه 🆕 — مرجع في 3 صفحات ❌**

> **ملاحظة:** مستوردة في `(website)/page.tsx` + `services/page.tsx` + `portfolio/page.tsx` — بتكسر الـ Build ❌

**المكونات:**
- عنوان تحفيزي: "ابدأ مشروعك الآن" / "Start Your Project Today"
- فقرة قصيرة مشجعة
- زر CTA كبير: "تواصل معنا" → يروح لصفحة التواصل

**مصدر البيانات (CMS) 📡:**
- `pages` (slug: `home`) → `content.cta_title`, `content.cta_text`, `content.cta_button` (EN/AR)

**التصميم:**
- خلفية: gradient من `#022026` لـ `#F2CB05/10`
- الزر: `bg-[#F2CB05]` مع `text-[#022026]` — حجم كبير

---

## الهيكل التقني

### تحليل الكود الموجود

> **ملاحظة مهمة:** بعد فحص الكود اتضح إن فيه صفحات كتير بتستورد components مش موجودة — ودي بتكسر الـ build.

#### الـ Components الموجودة فعلاً في `src/components/landing/`:

| الملف | الحالة |
|-------|--------|
| `index.ts` | ✅ موجود — بس بيصدّر `HeroSection` بس |
| `hero-section.tsx` | ✅ موجود — شغال |
| `hero-overlay.tsx` | ✅ موجود — شغال (يحتاج تعديل) |
| `countdown.tsx` | ✅ موجود — شغال |

#### الـ Components المستوردة بس مش موجودة (بتكسر الـ Build ❌):

| Component | مستوردة من أي صفحة | الملف مش موجود |
|-----------|-------------------|----------------|
| `Navbar` | 7 صفحات (website layout, about, contact, services, portfolio, terms, privacy) | ❌ |
| `Footer` | 7 صفحات (نفس المذكورة فوق) | ❌ |
| `ServicesSection` | `(website)/page.tsx` + `services/page.tsx` | ❌ |
| `PortfolioSection` | `(website)/page.tsx` + `portfolio/page.tsx` | ❌ |
| `TestimonialsSection` | `(website)/page.tsx` | ❌ |
| `CTASection` | `(website)/page.tsx` + `services/page.tsx` + `portfolio/page.tsx` | ❌ |

#### الصفحات الموجودة فعلاً (Server Components — inline content):

| الصفحة | المسار | المحتوى | ملاحظات |
|--------|--------|---------|---------|
| **About** | `src/app/[locale]/about/page.tsx` | قصتنا + مهمتنا + رؤيتنا | ✅ كامل — بيجيب data من Supabase `pages` table — بس بيستورد `Navbar`/`Footer` المش موجودين |
| **Contact** | `src/app/[locale]/contact/page.tsx` | فورم تواصل + معلومات اتصال | ✅ كامل — فيه phone/email/address — بس بيستورد `Navbar`/`Footer` المش موجودين |
| **Services** | `src/app/[locale]/services/page.tsx` | Header + "Why Choose DEX" section | ⚠️ جزئي — بيستورد `ServicesSection` و `CTASection` المش موجودين |
| **Portfolio** | `src/app/[locale]/portfolio/page.tsx` | Header + CTA | ⚠️ جزئي — بيستورد `PortfolioSection` و `CTASection` المش موجودين |
| **Terms** | `src/app/[locale]/terms/page.tsx` | شروط وأحكام | ✅ كامل — بس بيستورد `Navbar`/`Footer` المش موجودين |
| **Privacy** | `src/app/[locale]/privacy/page.tsx` | سياسة خصوصية | ✅ كامل — بس بيستورد `Navbar`/`Footer` المش موجودين |

#### الـ Layout:
- `src/app/[locale]/(website)/layout.tsx` — بيلف كل صفحات الموقع بـ `Navbar` + `Footer` + `SiteSettingsProvider`
- الـ Root page (`src/app/[locale]/page.tsx`) — دي الصفحة الشغالة حالياً — بتعرض `HeroSection` بس (بدون Navbar/Footer)

---

### الملفات المطلوب إنشاؤها

```
src/components/landing/
├── index.ts                    ← تحديث (تصدير كل الأقسام) — يحل مشكلة الـ imports المكسورة
├── hero-section.tsx            ← موجود ✅ (يحتاج تعديلات بسيطة)
├── hero-overlay.tsx            ← موجود ✅ (يحتاج تعديل layout)
├── countdown.tsx               ← موجود ✅
├── navbar.tsx                  ← 🆕 ⚡ أولوية عالية (7 صفحات معطلة بسببه)
├── footer.tsx                  ← 🆕 ⚡ أولوية عالية (7 صفحات معطلة بسببه)
├── services-section.tsx        ← 🆕 (مطلوب لـ 2 صفحة)
├── portfolio-section.tsx       ← 🆕 (مطلوب لـ 2 صفحة)
├── testimonials-section.tsx    ← 🆕 (مطلوب لـ 1 صفحة)
├── cta-section.tsx             ← 🆕 (مطلوب لـ 3 صفحات)
├── about-section.tsx           ← 🆕 (للـ landing page الرئيسية)
├── stats-section.tsx           ← 🆕 (للـ landing page الرئيسية)
├── contact-section.tsx         ← 🆕 (للـ landing page الرئيسية)
└── sidebar-menu.tsx            ← 🆕 (for hamburger menu)
```

### ملفات i18n المطلوب تحديثها

```
src/i18n/messages/
├── en.json    ← إضافة قسم "landing" كامل
└── ar.json    ← إضافة قسم "landing" كامل
```

### التقنيات المستخدمة

| التقنية | الاستخدام |
|---------|-----------|
| **Next.js 15** | Framework |
| **React 19** | UI Library |
| **Tailwind CSS 4** | Styling |
| **Framer Motion** | Animations & scroll-triggered effects |
| **React Three Fiber** | 3D Scene (Hero) |
| **Lucide React** | Icons |
| **Shadcn/ui** | UI Components |
| **next-intl** | i18n (عربي/إنجليزي) |
| **Supabase** | Backend (form submissions, notifications) |

---

## خطة التنفيذ (مراحل)

### المرحلة 0: إعادة هيكلة رائد الفضاء 🧑‍🚀🔴 — **الأولوية الأعلى**
> المدة المتوقعة: نص يوم

> **ده أول حاجة نعملها — لأنه أساس تجربة الـ Hero Section**

1. **تعديل `Astronaut.tsx`:**
   - نقل الرائد من `[-2.2, -4.8, 0]` إلى `[1.0, -3.5, -0.5]` (يمين الوسط)
   - تكبير Scale من `2.5` إلى `3.0`
   - تقليل Rotation Y من `0.4` إلى `0.15` (مواجه أكتر)
   - تحسين الإضاءة (Key: 200, Fill: 150, Rim: 80)
   - إضافة PointLight بنفسجي على الخوذة (visor glow)
   - إضافة emissive بنفسجي على مادة الخوذة
   - إضافة Sprite/Plane هالة مضيئة خلف الرأس
   - تحسين حركة الطفو (أبطأ + ميلان خفيف)

2. **تعديل `SceneCanvas.tsx`:**
   - Camera position: `[0, 0.3, 4.0]`
   - FOV: `40` (كان 45)
   - Bloom intensity: `0.7` / threshold: `0.15`

3. **تعديل `hero-overlay.tsx`:**
   - `items-end` → `items-start` (النص يروح الشمال)
   - `max-w-2xl` → `max-w-lg` (مساحة أقل عشان الرائد يبان)

4. **اختبار النتيجة** على Desktop و Mobile

---

### المرحلة 1: إصلاح الـ Build المكسور + تجهيز البنية 🏗️🔴
> المدة المتوقعة: يوم واحد
> **أولوية قصوى — 7 صفحات مكسورة بسبب imports مش موجودة**

1. **إنشاء `navbar.tsx`** — ⚡ يحل 7 صفحات مكسورة
2. **إنشاء `footer.tsx`** — ⚡ يحل 7 صفحات مكسورة
3. **إنشاء `cta-section.tsx`** — يحل 3 صفحات
4. **تحديث `index.ts`** — تصدير كل الـ components
5. إنشاء هيكل الملفات الباقية (ولو placeholder)
6. تحديث ملفات i18n (en.json و ar.json)
7. إضافة smooth scrolling و scroll-snap

### المرحلة 2: تطوير الأقسام الأساسية 📐
> المدة المتوقعة: 3-4 أيام

1. **About Section** — من نحن
2. **Services Section** — خدماتنا (مع الكروت التفاعلية)
3. **Portfolio Section** — أعمالنا (مع الفلتر والـ lightbox)
4. **Stats Section** — الأرقام (مع count-up animation)

### المرحلة 3: تطوير الأقسام التفاعلية 🎯
> المدة المتوقعة: 2-3 أيام

1. **Testimonials Section** — آراء العملاء (carousel)
2. **Contact Section** — فورم التواصل (مع validation و Supabase integration)
3. **Footer Section** — التذييل
4. **Floating Navbar** — شريط التنقل العائم

### المرحلة 4: تحسينات Hero Section 🎬
> المدة المتوقعة: يوم واحد

1. تحديث اللوجو
2. تفعيل الهمبرجر منيو (Sidebar)
3. إضافة دعم عربي للنصوص
4. تحسين الأداء والـ responsive
5. Responsive الرائد على الموبايل (أصغر / شفافية أقل)

### المرحلة 5: التنقية والتحسين ✨
> المدة المتوقعة: 1-2 يوم

1. تحسين الأداء (Lazy loading, Image optimization)
2. اختبار على كل الأجهزة (Mobile, Tablet, Desktop)
3. اختبار RTL (اللغة العربية)
4. SEO optimization (meta tags, OG images)
5. Accessibility (a11y)
6. اختبار السرعة (Lighthouse)

---

## تفاصيل التصميم

### نظام الألوان التفصيلي

```css
/* الألوان الأساسية */
--bg-darkest:    #011118;   /* Footer */
--bg-darker:     #011a1f;   /* Alternating sections */
--bg-dark:       #022026;   /* Primary background */
--bg-lighter:    #033640;   /* Hover states, cards */

/* الألوان الثانوية */
--accent:        #F2CB05;   /* Primary accent (أصفر) */
--accent-glow:   #FFE664;   /* Glow effect */
--accent-dim:    #F2CB0533; /* 20% opacity borders */

/* النصوص */
--text-primary:  #FFFFFF;
--text-secondary: #FFFFFF99; /* 60% opacity */
--text-muted:    #FFFFFF40;  /* 25% opacity */
```

### أنيميشن Guidelines

| النوع | التفاصيل |
|-------|---------|
| **Scroll Reveal** | fade-up, duration: 0.6s, stagger: 0.1s |
| **Hover Cards** | scale(1.02), border-glow, duration: 0.3s |
| **Count Up** | من 0 للرقم الهدف, duration: 2s, ease: easeOut |
| **Carousel** | slide, duration: 0.5s, auto-play: 5s |
| **Parallax** | speed: 0.3-0.5x scroll |
| **Page Transitions** | fade, duration: 0.3s |

### Typography Scale

| العنصر | الحجم (Desktop) | الحجم (Mobile) | الخط |
|--------|----------------|----------------|------|
| Hero H1 | 8xl (96px) | 5xl (48px) | Playfair Display |
| Section Title | 5xl (48px) | 3xl (30px) | Playfair Display |
| Subtitle | 2xl (24px) | xl (20px) | Tajawal |
| Body | base (16px) | sm (14px) | Tajawal |
| Caption | sm (14px) | xs (12px) | Space Mono |
| Numbers | 6xl (60px) | 4xl (36px) | Space Mono |

---

## ملاحظات مهمة

1. **الأداء**: الـ 3D scene (Three.js) محملة فقط في الـ Hero Section ومش في بقية الصفحة لتجنب مشاكل الأداء
2. **RTL**: كل قسم لازم يدعم اتجاه RTL للعربي
3. **Mobile First**: التصميم يبدأ من الموبايل ويتكبر
4. **Lazy Loading**: كل الأقسام تحت الـ fold لازم تتحمل lazy
5. **SEO**: كل قسم يحتاج structured data مناسب
6. **Accessibility**: كل العناصر التفاعلية لازم يكون ليها aria labels

---

## الأصول المطلوبة (Assets)

| الأصل | الحالة | الملاحظات |
|-------|--------|-----------|
| لوجو DEX (SVG) | 🔲 مطلوب | للـ navbar والـ footer |
| صور المشاريع | 🔲 مطلوب | على الأقل 6-8 صور للـ portfolio |
| صور العملاء | 🔲 مطلوب | على الأقل 4-6 صور للـ testimonials |
| أيقونات الخدمات | ✅ متاح | Lucide React icons |
| نموذج رائد الفضاء 3D | ✅ متاح | `public/models/astronaut.glb` |
| صور رائد الفضاء 2D | ✅ متاح | `public/images/astronaut-*.png` |
| خط Tajawal | ✅ متاح | Google Fonts |
| خط Playfair Display | ✅ متاح | Google Fonts |
| خط Space Mono | ✅ متاح | Google Fonts |

---

> **آخر تحديث:** فبراير 2026  
> **المسؤول:** فريق التطوير — DEX Marketing
