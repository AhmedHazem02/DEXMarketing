'use client'

import { RoleSchedulePage } from '@/components/shared/role-schedule-page'

export default function EditorSchedulePage() {
    return <RoleSchedulePage title={{ ar: 'جدول المونتاج', en: 'Editing Schedule' }} />
}
